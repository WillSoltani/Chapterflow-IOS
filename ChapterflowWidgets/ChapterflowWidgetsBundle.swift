import WidgetKit
import SwiftUI

@main
struct ChapterflowWidgetsBundle: WidgetBundle {
    var body: some Widget {
        StreakWidget()
        ContinueReadingWidget()
        ProgressRingWidget()
        NextReviewWidget()
    }
}
