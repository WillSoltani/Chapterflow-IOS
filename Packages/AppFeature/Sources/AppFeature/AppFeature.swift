/// Placeholder public entry point for the AppFeature module.
///
/// AppFeature is the app's composition root. It depends on every other module
/// in the workspace and exposes ``AppRootView`` as the top-level UI.
public enum AppFeature {
    /// The name of this module. Useful as a smoke-test symbol.
    public static let moduleName = "AppFeature"
}
