# Run WP-CATALOG-01

Commit candidate first. Run validation and independent review against that immutable exact head,
resolve every P0/P1/P2 finding, and treat remediation as a new head that invalidates affected gates.

You are the sole owner for **WP-CATALOG-01 — Complete the resilient catalog-to-detail journey**.
Read current instructions, shared program policies, package artifacts, live LibraryFeature/AppFeature
routes and tests, and the selected live skills. Reverify exact bases, owner checkout, worktrees/PRs,
all declared resource-lock slots, and frozen PR #117.

Claim the package atomically and create one isolated iOS worktree/branch. Edit only the declared
LibraryFeature and exact UpgradeEvidence test paths. Establish red proof, then implement the bounded
Home/Library/Search → Discover → Book Detail → exact Start or Continue outcome, including cache/partial
truth, cancellation, account switching, package-local localization, and the mandatory native matrix.
Do not decide top-level IA or grant access locally.

Run every exact AC selector and supporting lane through the external evidence wrapper. Each selector
must match at least one test with zero failures/skips/waivers. Create the candidate commit before full
validation/review; any change creates a new head. Push/open/merge one focused PR only when every
predicate holds. Never touch release systems or PR #117. Report exact revisions, evidence, risks,
preservation, and cleanup.
