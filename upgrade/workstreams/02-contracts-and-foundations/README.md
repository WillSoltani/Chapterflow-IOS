# Contracts And Foundations

Make source-derived contracts executable before affected vertical work.

## Packages

- [WP-CONTRACT-02/Pin executable contracts for the broken central-loop operations](./WP-CONTRACT-02/SPEC.md) — Account deletion, Ask, narration-plan, and notebook operations have source-derived canonical fixtures and drift checks that expose every iOS/backend incompatibility before feature implementation.

## Boundary

This workstream owns 1 outcome package. Packages still obey their own paths, dependencies, decisions, locks, validation, merge, and rollback contracts; the directory is not a shared write claim.
