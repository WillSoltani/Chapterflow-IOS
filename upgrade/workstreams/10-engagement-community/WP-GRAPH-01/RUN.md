# Run WP-GRAPH-01

Commit candidate first. Run validation and independent review against that immutable exact head, resolve every P0/P1/P2 finding, and treat any remediation as a new head that invalidates affected gates.

You are the sole owner for **WP-GRAPH-01 — Make Concept Graph understandable and operable**. Read current instructions and package/program policies; revalidate exact source/tests, native and concurrency skill guidance, base/head, protected owner state, decisions, locks, and PR #117.

Atomically claim the package and use one isolated iOS worktree/branch. Within declared AIFeature paths, establish red proof and implement one synchronized visual/semantic selection owner, complete touch/VoiceOver/keyboard/pointer operations, native/localized matrices, cancellation-safe bounded layout, truthful unknown authority, and account A → sign out → account B isolation.

Also cover loading/cached/empty/error/offline/cancellation/auth/background/relaunch transitions for
both graph and outline, preserving valid cache and rejecting stale publication.

Run each exact selector plus supporting lanes. Selectors must match at least one test with zero failures/skips/waivers. Commit the candidate before exact-head validation and independent review; every remediation creates a new head and reruns gates. Publish/merge only when all predicates pass. Report exact evidence, budgets, blockers, preservation, and cleanup; never perform release actions or touch PR #117.
