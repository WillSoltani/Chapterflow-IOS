# Validate WP-NATIVE-01

Record every command or scenario as `passed`, `failed`, `skipped`, `blocked`, or `not run`. A required selector passes only when it reports `matched >= 1`, `failed = 0`, and `skipped = 0`; zero matching selectors fail, and disabled or known-issue waivers are prohibited.

## Acceptance evidence

| AC | Assertion ID | Exact command and selector | Expected oracle | Required artifact |
|---|---|---|---|---|
| AC-NATIVE-01-01 | NATIVE-01-UNIT-01 | `swift test --package-path Packages/DesignSystem --filter BaselineValidatorTests` | placeholder, equivalent Light/Dark, missing, and dimension mismatch fixtures each fail with the declared reason | `results/native/baseline-negative.json` with candidate SHA and match/pass/skip counts |
| AC-NATIVE-01-02 | NATIVE-02-UI-01 | `xcodebuild test -project ChapterFlow.xcodeproj -scheme ChapterFlow -derivedDataPath /private/tmp/Chapterflow-DD-native-baseline-<SHA> -destination 'platform=iOS Simulator,id=<PINNED_UDID>' -resultBundlePath results/native/light-dark.xcresult -only-testing:ChapterFlowUITests/NativeEvidenceTests/testLightDarkBaselinesAreDimensionBound -parallel-testing-enabled NO CODE_SIGNING_ALLOWED=NO` | stable content metadata binds inputs/head/dimensions and required adaptive semantic differences exist | `results/native/light-dark.xcresult` plus baseline manifest/digests |
| AC-NATIVE-01-03 | NATIVE-03-UI-01 | `python3 scripts/visual/run_native_matrix.py --project ChapterFlow.xcodeproj --scheme ChapterFlow --test ChapterFlowUITests/NativeEvidenceTests/testCompactIPadAXPreserveRequiredContent --iphone-udid <PINNED_IPHONE_UDID> --ipad-udid <PINNED_IPAD_UDID> --scenarios scripts/visual/native-matrix.json --derived-data /private/tmp/Chapterflow-DD-native-matrix-<SHA> --require-dimensions light,dark,compact-iphone,regular-ipad,accessibility,voiceover,increased-contrast,reduce-motion,reduce-transparency,real-locale,pseudo-long,rtl,keyboard-pointer --output results/native/native-matrix` | every required dimension preserves required content/actions with no clipping, overlap, or unreachable focus | `results/native/native-matrix/manifest.json` plus pinned iPhone and iPad `.xcresult` bundles and geometry assertions |
| AC-NATIVE-01-04 | NATIVE-04-UNIT-01 | `python3 scripts/localization/validate_matrix.py --manifest scripts/visual/native-matrix.json --manifest-key localizationMatrix --candidate <SHA> --output results/native/localization.json` | the complete retained static schema/catalog/token oracle covers real locale, pseudo-long, plurals/formatting, RTL order, and semantic media direction without claiming target execution | `results/native/localization.json` with nested-manifest digest, scenario IDs, and candidate SHA |
| AC-NATIVE-01-04 | NATIVE-04-BUILD-BOUNDARY-04 | `python3 scripts/visual/run_native_matrix.py --project ChapterFlow.xcodeproj --scheme ChapterFlow --extension-build-boundary --base <BASE_SHA> --candidate <SHA> --ordinary-configuration Debug --ordinary-configuration Release --evidence-configuration Debug --derived-data /private/tmp/Chapterflow-DD-native-extension-build-boundary-<SHA> --extension-evidence-host scripts/localization/NativeExtensionEvidenceHost.swift --extension-evidence-condition CF_NATIVE_EXTENSION_EVIDENCE_BUILD --source-exclusion-variable CF_NATIVE_EXTENSION_EXCLUDED_SOURCES --require-production-source ShareExtension:ShareExtension/ShareViewController.swift --require-production-source ActionExtension:ActionExtension/ActionViewController.swift --exclude-production-source ShareExtension:ShareViewController.swift --exclude-production-source ActionExtension:ActionViewController.swift --require-ordinary-host-exclusion --require-evidence-host-inclusion --require-evidence-production-source-exclusion --require-evidence-marker-separation --require-unchanged-info-plist ShareExtension/Info.plist --require-unchanged-info-plist ActionExtension/Info.plist --require-principal-class ShareExtension=ShareViewController --require-principal-class ActionExtension=ActionViewController --require-appex ShareExtension.appex --require-appex ActionExtension.appex --negative-build-case ShareExtension:missing-debug --negative-build-case ActionExtension:missing-debug --negative-build-case ShareExtension:missing-evidence-condition --negative-build-case ActionExtension:missing-evidence-condition --negative-build-case ShareExtension:missing-target-flag --negative-build-case ActionExtension:missing-target-flag --negative-build-case ShareExtension:dual-target-flags --negative-build-case ActionExtension:dual-target-flags --negative-build-case ShareExtension:cross-wired-target-flag --negative-build-case ActionExtension:cross-wired-target-flag --negative-build-case ShareExtension:controller-host-collision --negative-build-case ActionExtension:controller-host-collision --negative-build-case ShareExtension:release-evidence-reachability --negative-build-case ActionExtension:release-evidence-reachability --output results/native/extension-build-boundary` | ordinary unsigned Debug and Release builds include both production controllers, exclude the evidence host, lack its unique marker, retain unchanged Info.plists/principal classes, and produce both `.appex` products; the Debug evidence build includes the host, excludes both production controllers, contains the marker, preserves the same principals/Info.plists/products, and every named positive and malformed condition/source case executes and passes separately for Share and Action before runtime | `results/native/extension-build-boundary/manifest.json` plus exact base/candidate, per-target and per-configuration build logs/settings, source inventories, `.appex`/Info.plist/binary digests, marker scans, and retained per-case positive/negative result bundles |
| AC-NATIVE-01-04 | NATIVE-04-REPRESENTATIVE-05 | `python3 scripts/visual/run_native_matrix.py --project ChapterFlow.xcodeproj --scheme ChapterFlow --extension-representative-records --candidate <EXACT_CANDIDATE_SHA> --iphone-udid <PINNED_IPHONE_UDID> --scenarios scripts/visual/native-matrix.json --derived-data /private/tmp/Chapterflow-DD-native-extension-representative-<EXACT_CANDIDATE_SHA> --record shareextension-compact-iphone-light --record actionextension-compact-iphone-light --expected-record-count 2 --require-named-system-elements --require-installed-extension-display-names --stop-on-first-deterministic-mismatch --output results/native/extension-representative` | after a clear static review and successful build-boundary, exactly one Share and one Action record select the installed extension by discovered named system elements and installed display name; either deterministic mismatch stops before production-boundary | `results/native/extension-representative/manifest.json` with the two record IDs, element/display-name discovery trace, candidate binding, identities, and pass/fail/skip counts |
| AC-NATIVE-01-04 | NATIVE-04-PRODUCTION-BOUNDARY-03 | `xcodebuild test -project ChapterFlow.xcodeproj -scheme ChapterFlow -configuration Debug -derivedDataPath /private/tmp/Chapterflow-DD-native-extension-boundary-<EXACT_CANDIDATE_SHA> -destination 'platform=iOS Simulator,id=<PINNED_UDID>' -resultBundlePath results/native/extension-presentation-boundary.xcresult -only-testing:ChapterFlowUITests/NativeEvidenceTests/testExtensionPresentationResultInputSeparatesFixtureAndLegacyProduction -parallel-testing-enabled NO CODE_SIGNING_ALLOWED=NO SWIFT_ACTIVE_COMPILATION_CONDITIONS='$(inherited) CF_NATIVE_EXTENSION_EVIDENCE_BUILD' CF_NATIVE_EXTENSION_EXCLUDED_SOURCES='ShareViewController.swift ActionViewController.swift' CF_NATIVE_EVIDENCE_CANDIDATE=<EXACT_CANDIDATE_SHA>` | the test fails before execution when `CF_NATIVE_EVIDENCE_CANDIDATE` is missing, zero, malformed, or differs from the exact checked-out candidate; both real `.appex` products execute through a containing/system host; their evidence principals instantiate the actual target-local legacy, fixture, and typed seams under exactly one target flag; existing controller initializer signatures compile unchanged in ordinary builds; legacy void Save/Ask invokes no capture/completion callback, cannot enter committed/success or announce success, cannot dismiss/open, clears busy state, retains payload/note, and exposes localized retry/error plus Cancel; the typed seam remains consumable by WP-EXT-01 without a NATIVE view/catalog edit | `results/native/extension-presentation-boundary.xcresult` plus exact nonzero candidate binding, `.appex`/process/executable/Info.plist identities, per-target initializer signatures, callback invocation counts, state/announcement/completion traces, retained-input/retry evidence, exact build-setting/source-membership record, and match/pass/skip counts |
| AC-NATIVE-01-04 | NATIVE-04-EXTENSION-02 | `python3 scripts/visual/run_native_matrix.py --project ChapterFlow.xcodeproj --scheme ChapterFlow --test ChapterFlowUITests/NativeEvidenceTests/testShareActionLocalizedAccessibilityMatrix --candidate <EXACT_CANDIDATE_SHA> --iphone-udid <PINNED_IPHONE_UDID> --ipad-udid <PINNED_IPAD_UDID> --scenarios scripts/visual/native-matrix.json --derived-data /private/tmp/Chapterflow-DD-native-extensions-<EXACT_CANDIDATE_SHA> --require-dimensions light,dark,compact-iphone,regular-ipad,accessibility,voiceover,increased-contrast,reduce-motion,reduce-transparency,real-locale,pseudo-long,rtl,keyboard-pointer --extension-evidence-host scripts/localization/NativeExtensionEvidenceHost.swift --extension-evidence-condition CF_NATIVE_EXTENSION_EVIDENCE_BUILD --source-exclusion-variable CF_NATIVE_EXTENSION_EXCLUDED_SOURCES --exclude-production-source ShareViewController.swift --exclude-production-source ActionViewController.swift --require-appex ShareExtension.appex --require-appex ActionExtension.appex --require-containing-or-system-host-invocation --expected-record-count 62 --expected-xcresult-count 2 --require-system-input-source system --require-system-trait-values colorSchemeContrast,accessibilityReduceMotion,accessibilityReduceTransparency --require-fixture-input-source fixture --require-fixture-system-trait-claim none --require-observed-consequences localization,rtl,plural,formatting,accessibility --require-token-scoped-reset-observers --require-exact-payload-digest --require-exact-configuration-digest --require-extension-identity process,executable,Info.plist --stop-on-first-deterministic-mismatch --forbid-retry-greening --output results/native/extension-localization-matrix` | after successful build, representative, and production boundaries, all 62 declared records execute through containing/system host UI in each installed real `.appex`; real-system records report actual extension-process `colorSchemeContrast`, `accessibilityReduceMotion`, and `accessibilityReduceTransparency` with `inputSource=system` and claim a requested state only after a match plus independent rendered consequence; a mismatch fails, never passes/skips; behavior fixtures state `inputSource=fixture` and `systemTraitClaim=none`; observed localized values, RTL order, selected plural branch, formatted value, and accessibility/rendered consequences replace copied planned assertions; observers reset per record token; exact payload/configuration digests and process/executable/Info.plist identities bind every record; transaction claims remain `none` | `results/native/extension-localization-matrix/manifest.json` with exactly 62 records and exactly two xcresults (pinned iPhone and pinned iPad), system/fixture provenance, observed trait and consequence fields, observer reset/token traces, exact payload/configuration digests, process/executable/Info.plist identities, target/catalog digests, and match/pass/fail/skip counts |
| AC-NATIVE-01-05 | NATIVE-05-UNIT-01 | `swift test --package-path Packages/DesignSystem --filter SurfaceEvidenceClassifierTests` | each stable/OS-dependent fixture selects the required deterministic or semantic/manual evidence mode | `results/native/surface-classification.json` |
| AC-NATIVE-01-05 | NATIVE-05-PERF-RUNNER-02 | `python3 scripts/visual/run_paired_performance.py --self-test --self-test-budget-id PERF-READER-PAGINATION --self-test-budget-id PERF-GRAPH-INTERACTION --budget-manifest upgrade/program/performance-budgets.json --output results/native/paired-performance-runner` | runner parses and plans the complete canonical Reader and Graph consumer commands, selects each exact structured budget, enforces current-main-before-candidate order, pinned worktree/head/device/OS/toolchain/fixture, isolated builds, sample counts, xcresult/Hangs/SwiftUI retention, and command-scoped leasing, and rejects missing, unknown, mismatched, relaxed, duplicate, or legacy options | `results/native/paired-performance-runner/manifest.json` with both consumer fixtures, normalized command plans, budget fingerprints, and negative-test dispositions |
| AC-NATIVE-01-06 | NATIVE-06-INVENTORY-01 | `python3 scripts/localization/inventory.py --check --owners upgrade/workstreams --require-target-catalog ShareExtension/Localizable.xcstrings --require-target-catalog ActionExtension/Localizable.xcstrings --output results/native/localization-inventory.json` | every visible string and surface has an explicit package owner and catalog strategy; Share/Action entries have non-empty real-locale target-owned translations | `results/native/localization-inventory.json` with catalog/table/key, locale, non-English value digest, and owner package |
| AC-NATIVE-01-06 | NATIVE-06-TARGETS-01 | `python3 scripts/visual/touch_targets.py --check --min-default 44 --min-exception 28 --output results/native/touch-targets.json` | schema/inventory errors and WP-NATIVE-owned failures are zero; no unmeasured control is assumed 44 points; every unmeasured or sub-44 feature target remains `owner-closure-required` under a stable ID and owner, including `reader-toolbar.depth-option` and `reader-toolbar.tone-option` assigned to WP-READER-01 rather than reported compliant | `results/native/touch-targets.json` with verified-compliant, verified-exception, owner-closure-required, and unverified counts plus stable IDs, measurements/evidence source, owners, and exception log |
| AC-NATIVE-01-06 | NATIVE-06-TARGETS-REGRESSION-02 | `python3 scripts/visual/touch_targets.py --self-test --output results/native/touch-target-scanner-regressions.json` | exactly the 20 unique reviewed cases `direct-button`, `direct-non-button`, `trailing-frame`, `multiline-frame`, `label-frame`, `self-constant`, `designsystem-constant`, `partial-dimensions`, `sibling-frame-non-borrowing`, `nested-frame-non-borrowing`, `line-comments-ignored`, `block-comments-ignored`, `ordinary-strings-ignored`, `multiline-strings-ignored`, `previews-ignored`, `inactive-debug-ignored`, `active-ios-conditions`, `elseif-else-fail-closed`, `valid-adjacent-exception`, and `invalid-exception-forms` execute through the same production scanner seam as `--check`; the last case covers malformed, commented, string-contained, and nonadjacent exceptions; registry and executed IDs are identical and unique with `matched=20`, `passed=20`, `failed=0`, and `skipped=0`; missing, duplicate, silently unexecuted, altered, skipped, or expectation-drifted cases fail, while `reader-toolbar.depth-option` and `reader-toolbar.tone-option` remain WP-READER-01 owner closures | `results/native/touch-target-scanner-regressions.json` with the reviewed and executed case IDs, per-case fixture/expectation/observed digests and dispositions, registry digest, and exact match/pass/fail/skip counts |
| AC-NATIVE-01-07 | NATIVE-07-PROJECT-01 | `python3 scripts/visual/validate_upgrade_ui_test_membership.py --project ChapterFlow.xcodeproj/project.pbxproj --root ChapterFlowUITests/UpgradeEvidence --require-target ChapterFlowUITests --extension-evidence-host scripts/localization/NativeExtensionEvidenceHost.swift --require-extension-target ShareExtension --require-extension-target ActionExtension --forbid-extension-target ChapterFlow --forbid-extension-target ChapterFlowUITests --require-target-flag ShareExtension=CF_NATIVE_SHARE_EVIDENCE_TARGET --require-target-flag ActionExtension=CF_NATIVE_ACTION_EVIDENCE_TARGET --ordinary-excluded-source NativeExtensionEvidenceHost.swift --show-build-settings-project ChapterFlow.xcodeproj --scheme ChapterFlow --configuration Debug --require-build-setting ShareExtension:EXCLUDED_SOURCE_FILE_NAMES=NativeExtensionEvidenceHost.swift --require-build-setting ActionExtension:EXCLUDED_SOURCE_FILE_NAMES=NativeExtensionEvidenceHost.swift --output results/native/ui-test-membership.json` | the synchronized group compiles each declared exact package test file once; the evidence host has exactly one source membership in each extension, none in the app/UI-test target, mutually exclusive target flags, and ordinary evaluated Debug host exclusion in both extension targets; later packages need no project edit | `results/native/ui-test-membership.json` with project digest, exact test inventory, source-phase memberships, per-target flags, evaluated build settings/exclusion values, and target membership |
| AC-NATIVE-01-08 | NATIVE-08-A11Y-01 | `swift test --package-path Packages/DesignSystem --filter NativeAccessibilityPrimitiveTests` | VoiceOver values/traits/order/focus/announcements, non-color meaning, increased contrast, Reduce Motion/Transparency, and equivalent-access target behavior pass normal/adverse fixtures | `results/native/accessibility-primitives.json` with candidate SHA, scenario IDs, and match/pass/skip counts |

Every selector requires `matched >= 1`, `failed = 0`, `skipped = 0`, and no disabled/known-issue waiver. A screenshot, build, lint, or broad suite alone cannot satisfy an AC.

## Supporting gates

- `swift test --package-path Packages/DesignSystem --parallel`
- run the exact `NATIVE-06-TARGETS-REGRESSION-02` row above; it is the authoritative 20-case scanner regression gate and cannot be replaced by duplicated parsing tests or prose
- before runtime, require a clear static exact-head review; then run the literal rows in this order:
  `NATIVE-04-BUILD-BOUNDARY-04`, `NATIVE-04-REPRESENTATIVE-05`,
  `NATIVE-04-PRODUCTION-BOUNDARY-03`, and `NATIVE-04-EXTENSION-02`. Stop on the first deterministic
  mismatch; retry-based greening is prohibited. The production-boundary candidate argument is a
  required nonzero exact SHA, and the full matrix is exactly 62 records with exactly two xcresults
- run the expanded `NATIVE-07-PROJECT-01` row above; with build-boundary, its literal command is the authoritative ordinary Debug/Release, evidence/negative-build, build-settings, and source-phase membership gate and may not be replaced by descriptive prose or non-executable Markdown
- scenario/artifact JSON Schema validation, candidate-head intended-path/secret scan, `git diff --check`, independent review, required CI, merge ancestry, and post-merge CI
- `python3 -B upgrade/scripts/validate_upgrade_plan.py --package-diff WP-NATIVE-01 --base cdc68d05aed931b43668253bbf19f192f78e7ad8 --head d332ae2f9091026d879be909f631bcd31bc39c82 --require-candidate-binding` binds the parked continuation candidate's exact tree, canonical binary diff digest, 20 paths, per-group allocations, and unchanged 20-file/three-root maxima before implementation resumes
- `python3 -B upgrade/scripts/validate_upgrade_plan.py --package-diff WP-NATIVE-01 --base <BASE_SHA> --head <SHA>` requires the exact future 20-path after manifest—one `scripts/localization/scenarios.json` removal and one `scripts/localization/NativeExtensionEvidenceHost.swift` addition—plus the same ordered claims, allocations, and unchanged maxima without treating the parked identity as final runtime proof

Any silent baseline skip, unassigned inventory item, assumed target measurement, owner assignment reported
as compliance, unexplained target exception, fixture evidence reported as transaction proof, production
fixture reachability, legacy void callback invocation, false success/announcement/dismiss/open, retained
busy state or lost input, candidate mismatch, failed/skipped selector, or unresolved P0/P1/P2 blocks merge.
Source scanning, direct `.appex` launch, importing extension views into the main app or UI-test bundle,
hardcoded marker-only proof, static fixture claims, nondeterministic Action-extension discovery, production
evidence-host reachability, an evidence build that retains a production controller, or any artifact that
claims transaction/durability/dismiss/open truth is also merge-blocking.
Missing/zero/mismatched candidate identity; coordinate-only extension selection; self-attested spoken
VoiceOver, pointer, or manual evidence; a requested system state inferred from a fixture; a
preconfiguration mismatch reported passed/skipped; copied localization/RTL/plural/formatting/accessibility
assertions without observed consequences; non-reset or non-token-scoped observers; missing exact
payload/configuration digests; missing process/executable/Info.plist binding; any record count other than
62; any full-matrix xcresult count other than two; execution-order drift; a seventh correction path; or a
retry after the first deterministic mismatch is likewise merge-blocking.
