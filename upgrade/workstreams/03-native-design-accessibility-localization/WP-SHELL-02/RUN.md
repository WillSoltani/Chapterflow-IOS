# Run WP-SHELL-02

Commit candidate first. Run validation and independent review against that immutable exact head, resolve every P0/P1/P2 finding, and treat any remediation as a new head that invalidates affected gates.

You are the sole owner for **WP-SHELL-02 — Compose the approved adaptive editorial app shell**. Read current instructions, program/package policies, resolved D-IA-01, WP-NATIVE-01 and WP-CATALOG-01 interfaces, live AppFeature tests, and routed skills. Reverify exact base, protected owner checkout, all resource-lock slots, and PR #117.

Atomically claim the package and `composition-root`; create one isolated iOS worktree/branch. Edit only declared AppFeature paths. Establish red proof, then compose one approved compact/regular navigation model, exact feature routes, session/account transitions, and iOS 18/enhanced outcome parity. Do not redesign feature internals or add another router/state owner.

Run each AC selector plus supporting gates; require nonzero matches and zero failures/skips/waivers. Commit candidate first, validate and independently review that immutable head, and rerun on changes. Publish/merge only when all predicates pass. No release work or PR #117 mutation.
