# Run WP-JOURNEY-01

Commit candidate first. Run validation and independent review against that immutable exact head, resolve every P0/P1/P2 finding, and treat any remediation as a new head that invalidates affected gates.

You are the sole owner for **WP-JOURNEY-01 — Qualify the deterministic simulator product journey**. Read current instructions and all program/package policies. Reverify every dependency is integrated, exact bases/contracts, scenario IDs, native evidence contract, skills, protected owner state, locks, and PR #117.

Atomically claim the package and `simulator-device`; create one isolated iOS worktree/branch. Edit only deterministic UI/test-support/simulator-QA paths. Establish the source-proven fixture and scenario manifest, then implement exact central-loop, adverse-state, account-isolation, and native-matrix automation. Never patch a product defect here or claim signed-device/performance evidence.

Run every exact selector; require nonzero matches and zero failures/skips/waivers. Commit candidate first, validate and independently review that immutable head, then push/merge only under delivery predicates. Verify post-merge main contains the reviewed tree. Report WP-DEVICE-01 as next and device/performance as pending. No deployment, release, or PR #117 action.
