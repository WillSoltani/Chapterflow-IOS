# Validate WP-DEVICE-01

Record every command or scenario as `passed`, `failed`, `skipped`, `blocked`, or `not run`. A required selector passes only when it reports `matched >= 1`, `failed = 0`, and `skipped = 0`; zero matching selectors fail, and disabled or known-issue waivers are prohibited.

## Acceptance evidence

| AC | Assertion ID | Exact command and scenario selector | Expected oracle | Required artifact |
|---|---|---|---|---|
| AC-DEVICE-01-01 | DEVICE-01-MANIFEST-01 | `python3 scripts/qa/device/run_matrix.py --manifest scripts/qa/device/scenarios.json --candidate <SHA> --validate-only --output results/device/manifest.json` | schema is complete; every required scenario binds SHA/build/device class/OS/config/environment/budget/samples and redaction policy | `results/device/manifest.json` |
| AC-DEVICE-01-01 | DEVICE-01-SURFACES-02 | `python3 scripts/localization/inventory.py --check --owners upgrade/workstreams --candidate <SHA> --changed-visible-only --output results/device/changed-visible-surfaces.json` | final-candidate source and owner packages produce one complete, deduplicated inventory of all changed visible surfaces with target/catalog/route and central-versus-noncentral classification | `results/device/changed-visible-surfaces.json` with candidate SHA, source/package digests, and surface count |
| AC-DEVICE-01-02 | DEVICE-02-AUTH-01 | `python3 scripts/qa/device/run_matrix.py --scenario DEVICE-AUTH-KEYCHAIN-SIWA-A2B --candidate <SHA> --output results/device/auth.json` | signed relaunch/expiry/reauth/sign-out/A→B continuity and isolation pass with no secrets in evidence | `results/device/auth.json` plus signed-build provenance |
| AC-DEVICE-01-03 | DEVICE-03-ROUTING-01 | `python3 scripts/qa/device/run_matrix.py --scenario DEVICE-APNS-WIDGET-EXTENSION-ROUTING --candidate <SHA> --output results/device/routing.json` | acknowledgement, owner isolation, import-before-clear, kill recovery, and exact auth replay pass | `results/device/routing.json` |
| AC-DEVICE-01-04 | DEVICE-04-LIFECYCLE-01 | `python3 scripts/qa/device/run_matrix.py --scenario DEVICE-AUDIO-NETWORK-STORAGE-LIFECYCLE --candidate <SHA> --output results/device/lifecycle.json` | interruption/route/background/offline/reconnect/low-storage/relaunch preserve text fallback and durable truth | `results/device/lifecycle.json` |
| AC-DEVICE-01-05 | DEVICE-05-PERF-01 | `python3 scripts/qa/device/run_matrix.py --scenario DEVICE-PERF-EXACT-FINAL --candidate <SHA> --output results/device/performance.json` | declared launch/catalog-scroll/reader-scroll/reader-pagination/image/memory/energy/main-actor/audio/download budgets pass with required samples and variance | `results/device/performance.json` plus Instruments traces and pagination `.xcresult` bundles referenced by digest |
| AC-DEVICE-01-06 | DEVICE-06-VOICEOVER-01 | `python3 scripts/qa/device/run_matrix.py --scenario DEVICE-ACCESSIBILITY-VOICEOVER-CENTRAL-JOURNEY --candidate <SHA> --output results/device/voiceover-central-journey.json` | an operator on the declared device/OS performs every scripted gesture; observed focus/order/labels/values/traits/announcements/recovery match expected behavior | `results/device/voiceover-central-journey.json` with candidate/build/device-class/OS, operator role, Accessibility Inspector version, gesture log, expected/observed record, and redacted issue attachments |
| AC-DEVICE-01-06 | DEVICE-06-VOICEOVER-SURFACES-02 | `python3 scripts/qa/device/run_matrix.py --scenario DEVICE-ACCESSIBILITY-VOICEOVER-CHANGED-SURFACES --inventory attempt://<DEVICE_SURFACE_INVENTORY_ATTEMPT_ID>/results/device/changed-visible-surfaces.json --candidate <SHA> --output results/device/voiceover-changed-surfaces.json` | every final-candidate inventory-listed non-central auth/onboarding/paywall/account, notification/extension/offline, engagement/graph/social surface is observed on the declared physical device with expected/actual focus, speech, announcements, actions, and recovery; no generated, stale-inventory, mutable-latest, or simulator-only substitution passes | `results/device/voiceover-changed-surfaces.json` with referenced attempt/inventory digest, complete surface coverage, candidate/build/device-class/OS, operator role, Accessibility Inspector version, gesture logs, expected/observed records, and redacted issue attachments |

The runner must report one selected scenario, all required steps, `failed = 0`, `skipped = 0`, and no waiver for a passing AC. The VoiceOver row requires actual operator/device observation; a simulator accessibility audit or generated assertion alone cannot pass it. A missing device, credential, provider, or approved environment produces `blocked` and blocks completion.

## Supporting gates

- `python3 scripts/qa/device/run_matrix.py --manifest scripts/qa/device/scenarios.json --candidate <SHA> --all --output results/device/all.json`
- candidate/product-head identity check, privacy/secret scan, script unit tests, `git diff --check`, independent evidence review, required CI, merge ancestry, and post-merge CI

Any candidate drift, missing sample/budget/environment identity, private-data leak, failed/skipped required scenario, unresolved P0/P1/P2, or product source change blocks merge and returns work to the owning package.
