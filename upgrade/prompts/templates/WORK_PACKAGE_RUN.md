# Run WP-<DOMAIN>-<NN>

You are the sole owner for one package. Read live instructions, `upgrade/SKILLS.md`, and all package
artifacts. Reverify base/backend/deployed evidence, dependencies, decisions, paths, locks, tools,
worktrees/PRs, protected owner state, and frozen PR #117. Create/reuse exactly one isolated
worktree/branch per affected repository declared in package metadata.

Establish deterministic red proof when feasible; implement only the spec within its envelope. Run
pre-commit intended-path/privacy checks and `git diff --check`, then commit candidate first. Bind
`VALIDATE.md`, affected dependents, and independent review to that immutable exact head. Resolve all
P0/P1/P2 and rerun invalidated gates; any remediation creates a new candidate head. Push/open one
focused PR per affected repository and auto-merge only under `DELIVERY_POLICY.md` and the declared
compatibility/merge order. Verify affected post-merge CI; a backend source merge is not deployment.
Then safely clean package-owned resources.

Stop truthfully for collision, stale state, decision, contract, migration, device/credential/
authority, scope/lock expansion, test failure/weakening, head drift, or unresolved finding. Report
exact revisions, evidence, GitHub state, preservation, cleanup, and next ready package. Never claim
deployment or release readiness.
